- name: Install Apt Packages
  apt: name-{{item}}
  with_items:
    - "{ snapd }"
    - "{ remmina }"
    - "{ vim }"
    - "{ wget }"
    - "{ git }"
    - "{ zsh }"
    - "{ preload }"
    - "{ htop }"

- name: Install Snap Packages
  snap: name={{item}}
  with_items:
    - "{ --classic code }"
    - "{ starship }"
    - "{ discord }"



